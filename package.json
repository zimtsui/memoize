{
  "name": "@zimtsui/memoize",
  "version": "0.0.17",
  "exports": {
    "./share": "./build/share.js",
    "./wait": "./build/wait.js"
  },
  "description": "",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b ./src/tsconfig.json",
    "clean": "rm -rf ./build",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zimtsui/memoize.git"
  },
  "author": "Zim",
  "dependencies": {
    "immutable": "^5.1.4"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
