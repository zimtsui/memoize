{
  "name": "@zimtsui/memoize",
  "version": "0.0.3",
  "exports": "./build/memoize.js",
  "description": "",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b ./src/tsconfig.json",
    "clean": "rm -rf ./build",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zimtsui/cache.git"
  },
  "author": "Zim",
  "dependencies": {
    "immutable": "^5.1.4"
  }
}
